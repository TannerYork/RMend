/**
*  Firebase Javascrips Code
*/


export const logIn = () => {
  //Log the use in via firebase prebuild login/signup
}

export const logOut = () => {

}

export const checkUserStatus = () => {
  //Checks if user is an admin or not.
}

export const sendReport = (report) => {
  //Sends data for a report object to firestore and firebase storage
}

export const updateReportsList = () => {
  //Removes an item from the reports list firestore database
}

export const addReportsListListener = () => {
  //Watches for changes in the reports list database object and updates ui
}

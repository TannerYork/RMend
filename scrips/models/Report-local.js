export default class ReportData {
  constructor(sender, timestamp, img, location, details) {
    this.sender = sender,
    this.timestamp = timestamp,
    this.img = img,
    this.location = location,
    this.details = details
  }
}
